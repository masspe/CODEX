# Codex Clone 🚀

Questa è una demo di applicazione che genera codice via prompt, lo salva in Git e gestisce le release.

## Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

## Frontend
```bash
cd frontend
npm install
npm start
```
